# Copy to .env and adjust if needed.
# Matches the bundled docker-compose.yml (Postgres on host port 5433).
DATABASE_URL="postgresql://taskboard:taskboard@localhost:5433/taskboard?schema=public"

# Port the app (Next + WebSocket, same process) listens on.
PORT=3000
